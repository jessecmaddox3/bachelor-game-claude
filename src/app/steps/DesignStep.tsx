// PLACEHOLDER: replaced with the real Design step in Task 6.
import type { BoardState } from '../useBoard';
import type { FontMetrics, FontBuffers } from '../../engine';

export interface DesignStepProps {
  board: BoardState;
  metrics: FontMetrics;
  buffers: FontBuffers | null;
}

export function DesignStep(_props: DesignStepProps) {
  return <div>Coming in Task 6</div>;
}
